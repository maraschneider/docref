function renderOnlySearchResults(innerHTML) {
  const searchResults = document.getElementById('search-results');
  console.log(searchResults)
  searchResults.innerHTML = innerHTML

}
renderOnlySearchResults("<%= j render 'users/profile_page/profile_approvalcards_list' %>");
